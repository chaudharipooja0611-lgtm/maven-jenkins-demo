# Maven Jenkins Demo

A simple Java Maven project for Jenkins build automation practical.

## Build
mvn clean package

## Artifact
target/maven-jenkins-demo-1.0-SNAPSHOT.jar

## JUnit Report
target/surefire-reports/*.xml
